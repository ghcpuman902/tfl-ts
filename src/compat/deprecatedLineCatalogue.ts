import { Lines as lineCatalogue } from '../generated/meta/Line';

/**
 * @deprecated Import `{ Lines }` from `tfl-ts/meta` instead.
 * `tfl-ts/dist/generated/meta/Line.js` is kept so 2.11 callers still resolve.
 * It will be removed in the next major.
 */
export const Lines = lineCatalogue;
