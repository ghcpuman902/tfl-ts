/**
 * @deprecated Use `client.raw` from `tfl-ts`.
 * `tfl-ts/dist/generated/raw.js` is kept so 2.11 deep imports still resolve
 * after the facade moved to `rawClient.js`. It will be removed in the next major.
 */
export { RawClient } from '../generated/rawClient';
